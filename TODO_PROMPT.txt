CONTEXTE
RPVD Success — PWA React/Vite/Tailwind + Netlify Functions + Supabase + Gemini + Stripe.
Site actuellement en ligne : https://rpvdsuccess.netlify.app (compte Netlify badamhton@gmail.com, déploiement auto sur push GitHub).
Deux anciens sites sont bloqués par manque de crédits Netlify : incandescent-hamster-05d62b.netlify.app et rpvd-success-v2.netlify.app — ne pas y toucher, ne rien y déployer.
Lis PROJECT_HANDOFF.md à la racine du repo pour le contexte complet avant de commencer.

TÂCHES À FAIRE, DANS CET ORDRE

1. COURRIEL — priorité absolue, bloque une vraie fonctionnalité business
   RESEND_API_KEY et EMAIL_FROM n'ont jamais été configurés sur aucun des 3 sites successifs. Sans ça, aucun courriel ne part (codes d'essai, codes premium après achat, confirmation d'upgrade). Demande les valeurs à l'utilisateur (ne jamais inventer une clé), configure-les avec `netlify env:set` sur le site rpvdsuccess.netlify.app, force un rebuild, puis teste un vrai envoi (demande d'essai gratuit ou achat test) et confirme la réception.

2. META ADS — variables manquantes + campagne manuelle
   META_PIXEL_ID et META_CAPI_ACCESS_TOKEN n'ont jamais été configurés. Demande les valeurs à l'utilisateur, configure-les, force un rebuild. Une fois en place, fais un achat Stripe test et vérifie dans Meta Events Manager > Test Events que l'événement Purchase remonte bien (valeur/devise réelles). La campagne elle-même ("RPVD-Success-Acquisition-V1" ou équivalent) doit être créée manuellement par l'utilisateur dans Meta Ads Manager — jamais depuis Claude Code, par design (aucun token d'accès API ne doit être collé dans le chat).

3. SURVEILLER LE COMPTE NETLIFY
   Ce compte (badamhton@gmail.com) est un compte Free comme les deux précédents qui se sont bloqués ("Account credit usage exceeded"). Si un déploiement échoue avec une erreur Forbidden/403, vérifie d'abord ce message précis avant de chercher ailleurs. Si ça arrive : ne PAS migrer une 4e fois par réflexe — propose d'abord à l'utilisateur d'ajouter un moyen de paiement sur le compte existant (plus simple, ne casse aucune URL/webhook).

4. DOMAINE PERSONNALISÉ (optionnel, à proposer)
   Le site a changé d'URL 3 fois à cause des migrations Netlify. Un domaine personnalisé achetable réglerait ça définitivement — poser la question à l'utilisateur s'il veut en acheter un, plutôt que de décider seul (dépense).

5. VÉRIFICATIONS AVANT TOUTE ANNONCE PUBLIQUE / CAMPAGNE
   - Tester un achat réel de chaque forfait (Solo, Trio, Premium Solo, Premium Trio, upgrade Base→Premium) en mode test Stripe, confirmer la réception du courriel (dépend de la tâche 1).
   - Tester le flux complet upload → analyse → Arbre de Cheminement → sauvegarde bibliothèque avec une vraie photo d'exercice.
   - Vérifier que le lien magique de connexion fonctionne avec une vraie adresse courriel.
   - Nettoyer toute donnée de test créée dans Supabase (activation_codes, users) après vérification.

Ne pose pas de questions inutiles sur des choix déjà tranchés dans PROJECT_HANDOFF.md — exécute directement. Si une valeur réelle (clé, montant, texte) manque et ne peut pas être devinée, demande-la explicitement plutôt que d'inventer. Si une action touche de l'argent réel ou la création d'un nouveau compte/service tiers, confirme avec l'utilisateur avant d'agir.
